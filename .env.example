# Server
PORT=3001
NODE_ENV=development

# Database
DB_PATH=data/bottlelink.db

# API
API_PREFIX=/api

# Provider settings
PROVIDER_FETCH_TIMEOUT_MS=30000

# Frontend (Vite) — prefix VITE_ required to expose to the browser
VITE_API_BASE=/api
VITE_SERVER_PORT=3001
